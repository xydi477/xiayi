int a, b, c;
//逗号、分号只在后面加空格。

if (current_time >= MAX_TIME_VALUE)
a = b + c;
a *= 2;
a = b ^ 2;
//比较操作符, 赋值操作符"="、 "+="，
//算术操作符"+"、"%"，逻辑操作符"&&"、"&"，
//位域操作符"<<"、"^"等双目操作符前后加空格。

*p = 'a'; 			// 内容操作"*"与内容之间
flag = !isEmpty; 	// 非操作"!"与内容之间
p = &mem; 			// 地址操作"&" 与内容之间
i++; 				// "++","--"与内容之间
//"!"、"~"、"++"、"--"、"&"（地址运算符）
//等单目操作符前后不加空格。

p->id = pid;		// "->"指针前后不加空格
//"->"、"."前后不加空格。

if (a >= b && c > d)
//if、for、while、switch等与后面的括号间
//应加空格，使if等关键字更为突出、明显。 
